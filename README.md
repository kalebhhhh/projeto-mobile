# projeto-flutter
instagram-downloader
*

flutter create reel_downloader_app
python -m uvicorn app.main:app --reload
flutter run
flutter build apk --release
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload (subir servidor)

Próximo grande passo importante, reduzir drásticamente o consumo do backend para kilobytes. 08/09/2026
